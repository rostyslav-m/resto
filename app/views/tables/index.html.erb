<h1>Listing tables</h1>
 
<table>
  <tr>
    <th>No.</th>
    <th>Table name</th>
  </tr>
 
  <% @tables.each do |table| %>
    <tr>
      <td width="50"><%= table.number %></td>
      <td><%= link_to table.title, table_path(table) %></td>
    </tr>
  <% end %>

</table>

<%= link_to 'Add new table', new_table_path %>
